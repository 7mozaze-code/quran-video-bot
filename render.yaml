version: 1
services:
  - type: web
    name: quran-video-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python quran_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false